const { fork } = require('child_process');
const { join } = require('path');

const DEMO_URL = 'https://0380027y.index-education.net/pronote/';
const DEMO_USERNAME = 'CDI GALLO';
const DEMO_PASSWORD = 'Cle.05/Champo';

function test(type)
{
    fork(join(__dirname, 'fetch.js'), [DEMO_URL, DEMO_USERNAME, DEMO_PASSWORD, 'none', type]);
}

test('student')
